---
title: Traces
description: Record spans to monitor the production performance and success rates of operations within your mobile app.
sidebar_position: 2
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import AndroidContent from './_partials/_android.mdx';
import IosContent from './_partials/_ios.mdx';
import FlutterContent from './_partials/_flutter.mdx';
import ReactNativeContent from './_partials/_react-native.mdx';
import UnityContent from './_partials/_unity.mdx';
import WebContent from './_partials/_web.mdx';

import androidLimits from '@site/shared/limits/android.json';
import iosLimits from '@site/shared/limits/ios.json';
import crossPlatformLimits from '@site/shared/limits/cross-platform.json';
import webLimits from '@site/shared/limits/web.json';

# Traces

## Overview

Embrace's Traces solution gives you visibility into any app operation you'd like to track, including duration, success rate, and any contextual metadata collected at runtime that helps debug the root cause of your mobile app's performance issues. With this tool, you can quickly spot any bottlenecks in your app's architecture, pinpoint areas you need to troubleshoot with high precision, and ultimately deliver a truly optimized user experience.

See [this page](/product/traces/technical-details.md) for technical details and terminology definitions.

## Key Capabilities

With the Embrace Traces API, you can:

- Create real-time performance timers or record data for past operations.
  - For real-time tracing, a "stopwatch" concept lets you start and stop a span's recording manually.
  - To record past operations, you can specify the start and end times of your spans that you might have captured already.
  - You can mix and match real time and past events by specifying the start and end times when you start and stop your spans.
- Add child spans to a parent span to track sub-operations within an operation.
- Attach attributes, span events, and links to other spans to give a span further context.
  - Attributes allow you to specify string key-value pairs that can be useful for filtering, grouping, and deriving custom metrics.
  - Span events represent a point in time during the runtime of the span.
    - Attributes on a span event can be used to store data and metadata about the event that describe it beyond its name.
  - Links create unidirectional relationships between spans so that you can formally relate two spans that belong to different traces.
    - Attributes on a link can be used to store data and metadata that further describes the relationship.

## Limits

<Tabs groupId="platform" queryString="platform">
  <TabItem value="android" label="Android">

| Type | Limit |
|---|---|
| Max number of spans per session | {androidLimits.traces.maxSpansPerSession} |
| Max number of attributes per span | {androidLimits.traces.maxAttributesPerSpan} |
| Max number of events per span | {androidLimits.traces.maxEventsPerSpan} |
| Max number of attributes per event | {androidLimits.traces.maxAttributesPerEvent} |
| Max number of links per span | {androidLimits.traces.maxLinksPerSpan} |
| Max length of attribute keys | {androidLimits.traces.maxAttributeKeyLength} characters |
| Max length of attribute values | {androidLimits.traces.maxAttributeValueLength} characters |
| Max length of span names | {androidLimits.traces.maxSpanNameLength} characters |
| Max length of event names | {androidLimits.traces.maxEventNameLength} characters |

  </TabItem>
  <TabItem value="ios" label="iOS">

| Type | Limit |
|---|---|
| Max number of attributes per span | {iosLimits.traces.maxAttributesPerSpan} |
| Max number of events per span | {iosLimits.traces.maxEventsPerSpan} |
| Max number of attributes per event | {iosLimits.traces.maxAttributesPerEvent} |
| Max number of breadcrumbs per session | {iosLimits.traces.maxBreadcrumbsPerSession} |
| Max length of span names | {iosLimits.traces.maxSpanNameLength} characters |

  </TabItem>
  <TabItem value="flutter" label="Flutter">

| Type | Limit |
|---|---|
| Max number of spans per session | {crossPlatformLimits.traces.maxSpansPerSession} |
| Max number of attributes per span | {crossPlatformLimits.traces.maxAttributesPerSpan} |
| Max number of events per span | {crossPlatformLimits.traces.maxEventsPerSpan} |
| Max number of attributes per event | {crossPlatformLimits.traces.maxAttributesPerEvent} |
| Max length of attribute keys | {crossPlatformLimits.traces.maxAttributeKeyLength} characters |
| Max length of attribute values | {crossPlatformLimits.traces.maxAttributeValueLength} characters |
| Max length of span names | {crossPlatformLimits.traces.maxSpanNameLength} characters |
| Max length of event names | {crossPlatformLimits.traces.maxEventNameLength} characters |

  </TabItem>
  <TabItem value="react-native" label="React Native">

| Type | Limit |
|---|---|
| Max number of spans per session | {crossPlatformLimits.traces.maxSpansPerSession} |
| Max number of attributes per span | {crossPlatformLimits.traces.maxAttributesPerSpan} |
| Max number of events per span | {crossPlatformLimits.traces.maxEventsPerSpan} |
| Max number of attributes per event | {crossPlatformLimits.traces.maxAttributesPerEvent} |
| Max length of attribute keys | {crossPlatformLimits.traces.maxAttributeKeyLength} characters |
| Max length of attribute values | {crossPlatformLimits.traces.maxAttributeValueLength} characters |
| Max length of span names | {crossPlatformLimits.traces.maxSpanNameLength} characters |
| Max length of event names | {crossPlatformLimits.traces.maxEventNameLength} characters |

  </TabItem>
  <TabItem value="unity" label="Unity">

Unity traces use the underlying Android and iOS SDK limits. Select the Android or iOS tab above to see their respective limits.

  </TabItem>
  <TabItem value="web" label="Web">

| Type | Limit |
|---|---|
| Max number of spans per session | {webLimits.traces.maxSpansPerSession} |
| Max number of attributes per span | {webLimits.traces.maxAttributesPerSpan} |
| Max number of events per span | {webLimits.traces.maxEventsPerSpan} |
| Max number of attributes per event | {webLimits.traces.maxAttributesPerEvent} |
| Max length of attribute keys | {webLimits.traces.maxAttributeKeyLength} characters |
| Max length of attribute values | {webLimits.traces.maxAttributeValueLength} characters |
| Max length of span names | {webLimits.traces.maxSpanNameLength} characters |
| Max length of event names | {webLimits.traces.maxEventNameLength} characters |

  </TabItem>
</Tabs>

There are no limits to the number of child spans you can have per root span, provided the total number of spans does not exceed the per-session maximum.

### Exceeding limits

If you exceed the listed limits, the operation with the limit-exceeding call will truncate the limit-exceeding value, or fail in the case of API calls that would result in a count limit being exceeded.

## Feature details

- Span Names are **case-sensitive**
- Key Names are **case-sensitive** and are **alphanumeric ASCII characters**
- There are no limits on the duration of a span as long as the app is running.
- In the session timeline UI, a completed span will appear in the session in which it ended.

:::warning Internal prefixes
The `emb-` and `emb.` prefixes are reserved for internal Embrace span and attribute names, respectively. You should never create a span or attribute key name with `emb-` and `emb.` prefixes. Behavior in such cases is undefined.
:::

## API usage examples

<Tabs groupId="platform" queryString="platform">
  <TabItem value="android" label="Android">
    <AndroidContent />
  </TabItem>
  <TabItem value="ios" label="iOS">
    <IosContent />
  </TabItem>
  <TabItem value="flutter" label="Flutter">
    <FlutterContent />
  </TabItem>
  <TabItem value="react-native" label="React Native">
    <ReactNativeContent />
  </TabItem>
  <TabItem value="unity" label="Unity">
    <UnityContent />
  </TabItem>
  <TabItem value="web" label="Web">
    <WebContent />
  </TabItem>
</Tabs>
